# 第二版验收记录

2026-09-06，Windows / Edge / Playwright。验收对象：同目录的独立 HTML 与 GLB。

| 范围 | 采样数 | 净空阻挡 | 缺少地面支撑 |
| --- | ---: | ---: | ---: |
| 主线、步行捷径、奖励支路 | 3066 | 0 | 0 |
| 升降梯上层出口 | 42 | 0 | 0 |
| 升降梯下层出口 | 42 | 0 | 0 |
| 升降梯井道扫掠 | 1458 | 0 | 不适用 |

步行路径每 0.2 m 采样，检测 1.2 m 宽、2.3 m 高的通行范围；脚下使用向下射线检查。箱体、柱体等凸体采用本地坐标半空间测试，布尔隧道采用射线奇偶性判断。升降梯上下平台分别停靠检查，井道按 3.2 m 宽范围逐层扫掠。

这是离散几何采样验收，不替代游戏引擎的连续碰撞检测。门闩属于可解锁对象，水与雾门属于视觉对象。

完整主线自动漫游已从区域 01 到达区域 09，并自动返回全景；浏览器未报告 JavaScript 错误。原路线约 373 m，第二版因增加平台外绕行和缓冲落点，约 465 m，导览约 111 秒。

全景、俯视、剖切、光照、路线、区域定位、捷径切换、暂停/退出、GLB 导出通过离线浏览器检查。桌面及手机构图已实拍检查。手机页面已增加全景取景范围。

模型改动包括：

- 将爬升/下降段移到平台外，增加平层落点，消除坡道与实心底座交叉。
- 路口栏杆按其他道路的通行范围留洞，支撑柱避让下层道路。
- 礼拜堂地基先实体合并，再切出贯通隧道，减少内部重合面。
- 上层楼板设置真实升降梯井口；井柱、梯台和出口分开建模。
- 房屋改为空心带门洞结构；重建石拱、双坡屋顶、桥下拱券与池内旱道。
- 调整水面与地面高度，处理楼板侧面重叠和井柱悬空装饰。

独立 HTML 不依赖外部 CDN。GLB 2.0 容器头、长度与实际导出文件一致。
依赖审计：`npm audit --omit=dev --audit-level=moderate`，0 vulnerabilities。

本地保留了上一版的 HTML、模型和源码；这一版的截图、详细 JSON 及测试脚本保留在工作目录中。
